import { NotFoundContent } from "@/components/layout/NotFoundContent";

/** 404 dentro do site: o layout de (site) ja traz header e rodape. */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
